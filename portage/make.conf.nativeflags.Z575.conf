# overrides:

#for Z575
#COMMON_NATIVE_FLAGS="-march=native"  #why not mtune?
COMMON_NATIVE_FLAGS="-march=native -mtune=native"
COMMON_NATIVE_RUSTFLAGS="-C target-cpu=amdfam10"  #better than "native" which would set it to "generic"
#see it with rustc -C target-cpu=help|head -3 OR target-cpu=help|head -3  OR 

#keep last
Znative=1
