#!/bin/bash
#^ for syntax

#PATH="/usr/lib/ccache/bin:${PATH}"
#^ redundant, either way only the compiling for the bootstrap gcc will be done with ccache! TODO: find a way to use ccache even with the compiled-bootrap compiler that gcc uses to build itself!
