#FIXME: firefox can't find hosts that are after a line that's longer than 990 bytes(not chars or graphemes), since it's not using glibc's lookup functions but its own, in nsprpub/pr/src/misc/prnetdb.c !
#^ from ArchLinux
