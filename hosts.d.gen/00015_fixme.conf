#FIXME: any hostnames with 0.0.0.0 address effectively resolve/map to localhost, so firefox can access your localhost ports if it knows any of your hostnames from /etc/hosts here that are like "0.0.0.0 bruh", see info for the line "bogus-nxdomain=0.0.0.0" in /etc/hosts

